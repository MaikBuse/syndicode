name: Release Binaries

on:
  release:
    types: [published]

permissions:
  contents: write

jobs:
  build-release:
    name: Build and Upload Release Binaries
    strategy:
      # Ensure all jobs run to completion, even if one fails.
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            binary_name: syndicode-client
          - os: ubuntu-latest
            target: aarch64-unknown-linux-gnu
            binary_name: syndicode-client
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            binary_name: syndicode-client.exe
          - os: macos-latest
            target: x86_64-apple-darwin
            binary_name: syndicode-client
          - os: macos-latest
            target: aarch64-apple-darwin
            binary_name: syndicode-client

    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable
          targets: ${{ matrix.target }}

      - name: Install protoc
        uses: arduino/setup-protoc@v3
        id: protoc
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}

      # Cache dependencies to speed up builds.
      # IMPORTANT: We do NOT cache the `target` directory itself to avoid inconsistent builds.
      - name: Cache Cargo dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
          key: ${{ runner.os }}-${{ matrix.target }}-cargo-${{ hashFiles('**/Cargo.lock') }}

      - name: Install build dependencies (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc-aarch64-linux-gnu
          sudo apt-get install -y libssl-dev pkg-config

      - name: Build binary
        run: cargo build -v --release --package syndicode-client --target ${{ matrix.target }}
        env:
          PROTOC: ${{ steps.protoc.outputs.protoc-path }}
          CARGO_TARGET_AARCH64_UNKNOWN_LINUX_GNU_LINKER: aarch64-linux-gnu-gcc

      - name: Prepare binary for packaging
        run: |
          mkdir staging
          cp target/${{ matrix.target }}/release/${{ matrix.binary_name }} staging

      - name: Package binary (Windows)
        if: runner.os == 'Windows'
        run: |
          Compress-Archive -Path staging/* -DestinationPath "syndicode-client-${{ github.ref_name }}-${{ matrix.target }}.zip"
        shell: pwsh

      - name: Package binary (Linux/macOS)
        if: runner.os == 'Linux' || runner.os == 'macOS'
        run: |
          tar czvf "syndicode-client-${{ github.ref_name }}-${{ matrix.target }}.tar.gz" -C staging .

      # --- DEBUGGING STEP ---
      # List files in the current directory to verify the archive was created.
      - name: Verify packaged files
        run: ls -R
        shell: bash

      - name: Upload Release Asset
        uses: actions/upload-release-asset@v1
        with:
          upload_url: ${{ github.event.release.upload_url }}
          asset_path: ./syndicode-client-${{ github.ref_name }}-${{ matrix.target }}.*
          asset_name: syndicode-client-${{ github.ref_name }}-${{ matrix.target }}.${{ runner.os == 'Windows' && 'zip' || 'tar.gz' }}
          asset_content_type: ${{ runner.os == 'Windows' && 'application/zip' || 'application/gzip' }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
